# Claude instructions

## Language
- Communicate with the user in Swedish
- All code, files, comments and documentation must be in English

- See `doc/REQUIREMENTS.md` for technical and design requirements.
- Site is bilingual: `/sv/` and `/en/`
- Content: separate `content.sv.yaml` and `content.en.yaml` — **always update both**
- `resume.yaml` is bilingual per field: `description: {sv: ..., en: ...}`
- `resume.yaml` is used for purposes beyond this site — keep it clean CV data only
